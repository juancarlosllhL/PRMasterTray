/// Namespace for package-level metadata.
///
/// The domain types (`PullRequest`, `Readiness`, `GitHubClient`, …) land here in
/// subsequent stories; this exists so the target has a source file and the test
/// target has something to import.
public enum PRMasterCore {
    public static let version = "0.1.0"
}
